import {TingsSlice} from "../types"

export const slice: TingsSlice = {
    initted: false,
    initting: false,
    subscribed: false,
    dialogOpen: false,
    fbId: null,
    ting: null,
}

export default slice