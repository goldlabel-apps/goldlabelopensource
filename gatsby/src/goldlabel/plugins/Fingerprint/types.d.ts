export type FingerprintSlice = {
    fullscreen: boolean
    title: string
}

export type FingerprintType = {
    fingerprint: string
    host: string
    displayName: string
    ip: string
    lat: number
    lng: number
    zoom: number
}
