import {TingsSlice} from "../types"

export const slice: TingsSlice = {
    dialogOpen: false,
    initted: false,
    initting: false,
    subscribed: false,
    ting: null,
    fbTing: null,
}

export default slice
