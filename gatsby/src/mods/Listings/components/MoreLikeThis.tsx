import * as React from 'react'
import {
  Box,
} from "@mui/material"

import {
  Icon,
  Font,
} from "../../../goldlabel"

export default function MoreLikeThis(props: any) {
  const {frontmatter} = props
  return (
    <Font variant="small">
      More Like This
    </Font>
  )
}
